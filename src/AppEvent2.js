import React, { Component } from 'react';

function AppEvent2() {
  return (
    <button onClick={() => alert('Hello from Inline Code')}>
      Click me!
    </button>
  );
}

export default AppEvent2;